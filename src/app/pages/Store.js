import GSAP from 'gsap';

import Page from '@/classes/Page';

export default class StorePage extends Page {
  constructor() {
    super({
      element: '.store',
      elements: {

      },
    });
  }
}
